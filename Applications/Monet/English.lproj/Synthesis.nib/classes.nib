{
    IBClasses = (
        {
            CLASS = EventListView; 
            LANGUAGE = ObjC; 
            OUTLETS = {mouseTimeField = NSTextField; mouseValueField = NSTextField; }; 
            SUPERCLASS = NSView; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {print = id; }; 
            CLASS = IntonationScrollView; 
            LANGUAGE = ObjC; 
            OUTLETS = {scaleView = NSView; }; 
            SUPERCLASS = NSScrollView; 
        }, 
        {
            ACTIONS = {loadContour = id; loadContourAndUtterance = id; saveIntonationContour = id; }; 
            CLASS = IntonationView; 
            LANGUAGE = ObjC; 
            OUTLETS = {controller = AppController; smoothing = id; utterance = id; }; 
            SUPERCLASS = NSView; 
        }, 
        {
            ACTIONS = {
                generateContour = id; 
                generateGraphImages = id; 
                parseStringButton = id; 
                setBeatOffset = id; 
                setHertz = id; 
                setSemitone = id; 
                setSlope = id; 
                showIntonationParameterWindow = id; 
                showIntonationWindow = id; 
                synthesizeToFile = id; 
                synthesizeWithSoftware = id; 
                synthesizeWithSoftware2 = id; 
            }; 
            CLASS = MSynthesisController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                absTimeTextField = NSTextField; 
                beatOffsetTextField = NSTextField; 
                beatTextField = NSTextField; 
                driftCutoffField = NSTextField; 
                driftDeviationField = NSTextField; 
                eventListView = EventListView; 
                filenameField = NSTextField; 
                hertzTextField = NSTextField; 
                intonParmsField = NSForm; 
                intonationMatrix = NSMatrix; 
                intonationParameterWindow = NSWindow; 
                intonationRuleTableView = NSTableView; 
                intonationView = NSScrollView; 
                intonationWindow = NSWindow; 
                parameterTableView = NSTableView; 
                parametersStore = NSButton; 
                radiusMultiplyField = NSTextField; 
                semitoneTextField = NSTextField; 
                slopeTextField = NSTextField; 
                smoothIntonationSwitch = NSButton; 
                stringTextField = NSTextField; 
                tempoField = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}