{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {parseRule = id; shiftPosturesLeft = id; }; 
            CLASS = MRuleTester; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                consumedTokensTextField = NSTextField; 
                durationOutputForm = NSForm; 
                posture1Form = NSForm; 
                posture2Form = NSForm; 
                posture3Form = NSForm; 
                posture4Form = NSForm; 
                ruleOutputTextField = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}